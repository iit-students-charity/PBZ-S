<%= form_with(model: tariff, local: true) do |form| %>
  <% if tariff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tariff.errors.count, "error") %> prohibited this tariff from being saved:</h2>

      <ul>
        <% tariff.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :special_price %>
    <%= form.text_field :special_price %>
  </div>
  
  <div class="field">
    <%= form.label :expire_date %>
    <%= form.datetime_select :expire_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
